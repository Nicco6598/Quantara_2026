packages:
  - "apps/*"
  - "packages/*"

# Supply-chain guardrail: do not resolve npm versions published in the last 7 days.
minimumReleaseAge: 10080
minimumReleaseAgeExclude:
  # Already locked transitive dependency; keep install reproducible while new packages age out.
  - jiti
