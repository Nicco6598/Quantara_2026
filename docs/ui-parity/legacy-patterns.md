# Legacy UI Parity

The first shell preserves the operational patterns visible in the legacy screenshots:

- persistent left sidebar with project context;
- topbar with date range, filters, export and primary SAL action;
- KPI cards in a dense top row;
- health and forecast widgets;
- central dense project table;
- right contextual panel with SAL economics and linked documents.
