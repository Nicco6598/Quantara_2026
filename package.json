{
  "name": "quantara",
  "version": "0.4.22",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": "24.11.0",
    "pnpm": "11.1.1"
  },
  "scripts": {
    "build": "pnpm version:sync && pnpm --filter @quantara/desktop build",
    "build:analyze": "VITE_MODE=analyze pnpm --filter @quantara/desktop build && node scripts/check-bundle-size.mjs",
    "check": "pnpm version:sync && pnpm typecheck && pnpm lint && pnpm test",
    "deps:dead": "pnpm --filter @quantara/desktop exec knip --reporter compact",
    "format": "biome format --write .",
    "format:check": "echo '--- FORMAT ---' && biome format . && echo '--- TYPECHECK ---' && pnpm -r typecheck && echo '--- LINT ---' && biome lint . && echo '--- TESTS ---' && vitest run && echo '--- BOUNDARIES ---' && node scripts/check-feature-boundaries.mjs",
    "lint": "biome lint .",
    "parser:bundle": "node scripts/prepare-rfi-parser-runtime.mjs",
    "release:prep": "pnpm install --frozen-lockfile && pnpm version:sync && pnpm parser:bundle && pnpm format:check && pnpm typecheck && pnpm lint && pnpm test && pnpm build",
    "release:tag": "node scripts/release-tag.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r typecheck",
    "release:notes": "node scripts/extract-release-notes.mjs",
    "version:check": "node scripts/sync-version.mjs --check",
    "version:sync": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@playwright/test": "1.60.0",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  }
}
